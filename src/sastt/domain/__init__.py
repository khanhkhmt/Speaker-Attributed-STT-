"""Domain layer — pure types and invariants (spec 0.3, 17).

This package MUST NOT import a model framework, a web framework or a storage
driver. Everything here is deterministic and unit-testable without weights.
"""
